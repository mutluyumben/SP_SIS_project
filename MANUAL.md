# User Manual
To run this project:
1. Ensure Docker and Docker Compose are installed.
2. Run the command: `docker-compose up --build`
3. The application will automatically connect to the PostgreSQL database.
