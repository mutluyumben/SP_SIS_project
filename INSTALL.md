# Installation Guide

## Dependencies
To build this project, you need the following packages:

```bash
sudo apt-get update
sudo apt-get install git g++ cmake libpqxx-dev -y
```

## How to Build
```bash
mkdir build
cd build
cmake ..
make
```
